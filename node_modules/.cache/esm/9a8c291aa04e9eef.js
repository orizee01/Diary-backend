let bcrypt,Chance;_a04‍.x([["hashPassword",()=>hashPassword],["generateToken",()=>generateToken],["generateOTP",()=>generateOTP],["default",()=>_a04‍.o]]);_a04‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_a04‍.w("chance",[["default",["Chance"],function(v){Chance=v}]]);/* eslint-disable import/no-extraneous-dependencies */




const chance = new Chance();

/**
 * This is used for generating a hash and a salt from a String.
 * @param {string} password - String to be encrypted.
 * @memberof Helper
 * @returns {Promise<string>} - A promise that resolves to the hashed password.
 */
       const hashPassword = (password) => bcrypt.hash(password, 10);

/**
 * This generates a token.
 * @param {number} length - The length of the token.
 * @memberof Helper
 * @returns {string} - Generated unique token.
 */
       const generateToken = (length) => chance.string({ length, alpha: true, numeric: true });

/**
 * This generates an OTP.
 * @param {number} length - The length of the OTP.
 * @memberof Helper
 * @returns {string} - Generated OTP.
 */
       const generateOTP = (length) => chance.string({ length, numeric: true });

// Export all functions as a default object
_a04‍.d({
  hashPassword,
  generateToken,
  generateOTP
});
