let promise,pg,monitor,config;_2b6‍.x([["default",()=>_2b6‍.o]]);_2b6‍.w("bluebird",[["default",["promise"],function(v){promise=v}]]);_2b6‍.w("pg-promise",[["default",["pg"],function(v){pg=v}]]);_2b6‍.w("pg-monitor",[["default",["monitor"],function(v){monitor=v}]]);_2b6‍.w("../../config/env",[["default",["config"],function(v){config=v}]]);/* eslint-disable no-unused-vars */
/* eslint-disable import/no-extraneous-dependencies */
// eslint-disable-next-line import/no-extraneous-dependencies





// console.log(config.DATABASE_URL, 'dfghjkl');

const camelizeColumns = (data) => {
  const template = data[0] || {};
  Object.keys(template).forEach((prop) => {
    const camel = pg.utils.camelize(prop);
    if (!(camel in template)) {
      data.forEach((el) => {
        const d = el;
        d[camel] = d[prop];
        delete d[prop];
      });
    }
  });
};

const options = {
  promiseLib: promise,
  receive: (data, _result, _e) => camelizeColumns(data),
};

monitor.setTheme('matrix');
monitor.attach(options);

const pgp = pg(options);
const db = pgp(config.DATABASE_URL);

_2b6‍.d(db);
