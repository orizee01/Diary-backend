let fs,morgan,bodyParser,helmet,FileStreamRotator,loggerInit,routes,errorHandler,config;_4f1‍.x([["default",()=>_4f1‍.o]]);_4f1‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_4f1‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);_4f1‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_4f1‍.w("helmet",[["default",["helmet"],function(v){helmet=v}]]);_4f1‍.w("file-stream-rotator",[["default",["FileStreamRotator"],function(v){FileStreamRotator=v}]]);_4f1‍.w("express-async-errors");_4f1‍.w("./logger",[["default",["loggerInit"],function(v){loggerInit=v}]]);_4f1‍.w("../routes",[["default",["routes"],function(v){routes=v}]]);_4f1‍.w("./errorHandler",[["default",["errorHandler"],function(v){errorHandler=v}]]);_4f1‍.w(".",[["default",["config"],function(v){config=v}]]);/* eslint-disable import/extensions */
/* eslint-disable import/no-extraneous-dependencies */









// eslint-disable-next-line no-unused-vars


const logDirectory = './log';
const checkLogDir = _4f1‍.a("fs",fs).existsSync(logDirectory) || _4f1‍.a("fs",fs).mkdirSync(logDirectory);

const expressConfig = (app) => {
  let accessLogStream;
  let logger;

  // initialize logger
  if (app.get('env') === 'development') logger = _4f1‍.a("loggerInit",loggerInit)('development');
  else if (app.get('env') === 'production') logger = _4f1‍.a("loggerInit",loggerInit)('production');
  else if (app.get('env') === 'test') logger = _4f1‍.a("loggerInit",loggerInit)('test');
  else logger = _4f1‍.a("loggerInit",loggerInit)();

  global.logger = logger;
  logger.info('Application starting...');
  logger.debug("Overriding 'Express' logger");

  if (checkLogDir) {
    accessLogStream = _4f1‍.a("FileStreamRotator",FileStreamRotator).getStream({
      date_format: 'YYYYMMDD',
      filename: `${logDirectory}/access-%DATE%.log`,
      frequency: 'weekly',
      verbose: false,
    });
  }

  app.use(_4f1‍.a("morgan",morgan)('combined', { stream: accessLogStream }));

  app.use(_4f1‍.a("bodyParser",bodyParser).json());
  app.use(_4f1‍.a("bodyParser",bodyParser).urlencoded({ extended: false }));

  // Use helmet to secure Express headers
  app.use(_4f1‍.a("helmet",helmet)());
  app.disable('x-powered-by');

  app.use((req, res, next) => {
    res.setHeader('Access-Control-Allow-Origin', '*');
    res.setHeader('Access-Control-Allow-Methods', 'GET, POST, OPTIONS, PUT, PATCH, DELETE');
    res.setHeader('Access-Control-Allow-Headers', 'Authorization, Origin, Content-Type, Accept');
    res.setHeader('Access-Control-Allow-Credentials', true);
    next();
  });

  app.use('/v1/', _4f1‍.a("routes",routes));
  app.use(_4f1‍.a("errorHandler",errorHandler));

  // catch 404 and forward to error handler
  app.use((req, res, next) => {
    const err = new Error('Not Found');
    err.status = 404;
    next(err);
  });

  // error handlers

  // development error handler
  // will print stacktrace
  if (app.get('env') === 'development' || app.get('env') === 'test') {
    app.use((err, req, res) => res.status(err.status || 500)
      .json({
        message: err.message,
        error: err,
      }));
  }

  // production error handler
  // remove stacktrace
  app.use((err, req, res) => res.status(err.status || 500).json({ message: err.message }));
};

_4f1‍.d(expressConfig);
