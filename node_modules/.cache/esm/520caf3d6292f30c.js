let _,CustomerService,Helpers,Response;_629‍.x([["default",()=>_629‍.o]]);_629‍.w("lodash",[["default",["_"],function(v){_=v}]]);_629‍.w("./customer.service",[["default",["CustomerService"],function(v){CustomerService=v}]]);_629‍.w("../../../utils/helper",[["default",["Helpers"],function(v){Helpers=v}]]);_629‍.w(".",[["Response",["Response"],function(v){Response=v}]]);/* eslint-disable import/no-named-as-default-member */
// eslint-disable-next-line import/no-extraneous-dependencies





/**
 * controllers that contains methods for managing customer endpoints
 * @class CustomerController
 */
class CustomerController {
  static async CustomerController(req, res) {
    // Destructure only the required field
    const { name, email, password, role } = req.body;

    // Normalize the email
    const trimmedEmail = email ? email.trim().toLowerCase() : null;

    // check if email already exists
    const customerExist = await _629‍.a("CustomerService",CustomerService).getCustomerByEmail({ email: trimmedEmail });
    if (customerExist) {
      return _629‍.a("Response",Response).error(res, 'Accout exist, Kindly proceed to the login Screen', 409);
    }
    // hashed the data before storing
    const hashedPassword = await _629‍.a("Helpers",Helpers).hashPassword(password);

    // Prepare the customer data for creation
    const CustomerData = {
      name,
      email: trimmedEmail,
      password: hashedPassword,
      role
    };

    // Create new Customer

    const customer = await _629‍.a("CustomerService",CustomerService).createCustomer(CustomerData);

    // send response with limited customer details
    _629‍.a("Response",Response).info(res, 'User created successfully!', 201, _629‍.a("_",_).pick(customer, ['name', 'role']));
  }
}
_629‍.d(CustomerController);
